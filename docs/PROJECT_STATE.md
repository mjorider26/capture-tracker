# Capture Tracker project state

Capture Tracker is a mobile-first S-corporation bookkeeping and financial-review application using Next.js App Router, PostgreSQL, Prisma, Better Auth, and server-derived business scope.

## Completed foundation

- Phases 1–6 established secure accounting, authentication and business authorization, deterministic fictional demo data, responsive Today/Money surfaces, and full PostgreSQL integrity/concurrency validation.
- Phase 7 added reconciliation and immutable journal reversals.
- Phase 8 added the Taxes workspace, owner compensation information, and database-backed external tax-payment recording.
- Phase 9A established the Cloudflare/OpenNext/Neon repository foundation without creating a provider resource.
- Phase 9B repository preparation added fictional-staging configuration guards, migration/bootstrap safeguards, future smoke and Neon verification tools, Linux CI preparation, bundle verification tooling, and boundary/recovery/cost documentation.
- Phase 9B.1 updated Next to 16.2.12, moved OpenNext to exact build-only tooling, removed the deferred R2 binding from fictional staging, and added dependency-separation release checks. Production audit improved from 12 high/1 moderate to 6 high/1 moderate; the gate was initially blocked by remaining Next/Prisma findings and missing Linux Worker artifact evidence.
- Phase 9B.2 isolated AWS CDK TypeScript from the root Next build and repaired the `pg-cloudflare@1.4.0` Workerd package boundary. The successful Linux OpenNext workflow now produces a 2,278-byte Worker entry below the 3 MiB guard and verifies driver packaging, configured tooling exclusions, and secret/data scans. PostCSS, Sharp, `find-my-way`, and Valibot do not yet have direct artifact-inventory/reachability evidence, so fictional staging remains decision B conditionally blocked.
- Phase 9B.3 adds sanitized Worker dependency inventory and request-entrypoint reachability tooling to the non-deploy Linux workflow. At `770668e`, Linux generated that inventory and the reachability gate rejected at least one targeted high package as request-time reachable. The private sanitized artifact must identify the exact package/path before a remediation claim; fictional staging is decision C blocked.
- Phase 9B.4 reviewed the attached artifact: `sharp@0.34.5` is only Next's unresolved conditional image-optimizer import, with no Sharp code copied into the Worker. Fixed local brand images retain `next/image` but now use the supported `images.unoptimized` configuration, avoiding the unapproved Cloudflare Images binding. The corrected artifact gate is decision B pending new Linux evidence and a fresh audit.
- Linux verification for `8fa8f87` passed the remediated OpenNext artifact inventory/reachability gate, size guard, driver packaging, secret/data scans, and isolated infrastructure checks. No Worker high is request-time reachable; fictional staging remains decision B only until a fresh authoritative audit and separately authorized deployment.
- Phase 9B.5 adds a fail-closed authoritative Linux runtime-audit report and advisory-to-Worker classification. Linux runs `30188607900` (`62bd602`) and `30188831742` (`be449e3`) passed all pre-audit artifact/security gates but failed closed at `cloud:audit:report`; the private sanitized artifacts were not accessed without repository-admin authorization. The local diagnostic endpoint is unavailable and is not treated as clean. No fresh audit counts or classifications replace the historical 6 high / 1 moderate record. Decision B remains until a successful Linux report is reviewed.
- Phase 10A adds a fictional metadata-only Documents foundation with scoped hashes, retention targets, immutable status history, a disabled storage boundary, and demo/application Documents routes. No file upload, provider, cloud resource, or real document data is included.
- Phase 10B adds a fictional-only PDF/JPEG/PNG upload workflow with signature-derived MIME checks, actual-byte SHA-256, private local namespaces, deterministic scanning, duplicate-safe storage compensation, short-lived actor/business/document-bound grants, and a protected private-content route. Its future R2 binding adapter is inactive and creates no provider resource. OCR, extraction, transaction linking, deletion, and deployment remain future work.
- Phase 10C adds business-isolated document-to-transaction relationships. A partial unique active-pair constraint, composite business foreign keys, and atomic link/unlink services preserve exactly one active relationship and immutable `LINKED` / `UNLINKED` history; relinking creates a new relationship without deleting the prior one. Authenticated Money and Documents detail pages expose only bounded eligible selectors and protected private opens. Links do not affect accounting data. The deterministic fictional demo now covers single, multiple, shared, unlinked, and unlink/relink relationship states without binary documents.
- Phase 10D adds provider-neutral structured document extraction review. Active clean private documents receive version-bound attempts, candidates, and immutable review history through a deterministic no-network fictional adapter. Stale evidence cannot be reviewed, and extraction is evidence-only: it makes no transaction or accounting changes. Production fails closed until a separately approved provider configuration exists.
- Phase 10E adds bounded deterministic transaction-match suggestions from current human-reviewed extraction evidence. Each score has retained reason codes and an immutable business-scoped run/decision history. Suggestions require explicit approval; only approval creates the existing Phase 10C relationship, and every source, evidence, document, transaction, or link change makes pending results stale. Matching never changes accounting data and has no external AI or provider dependency.
- Phase 11 adds a business-scoped Weekly Review with immutable start/completion/reopen evidence, unresolved-item acknowledgement, and read-only ledger-backed Profit and Loss, Balance Sheet, Trial Balance, and cash-activity reports. Reports use posted journal lines only and remain separate from accounting writes; CSV is a guarded future-export boundary.
- Phase 12 adds a business-scoped, read-only Ask AI conversation boundary with immutable run/evidence/feedback audit records, bounded trusted report and operational context, and a deterministic no-network fictional adapter. No AI provider, SDK, credential, endpoint, deployment, or real-data activation is configured; production fails closed pending separate approval.
- Phase 13 adds fictional-only onboarding, business settings with immutable change history, a bounded activity viewer, and authenticated audited safe CSV exports. These product-readiness surfaces never modify accounting records or relax provider, deployment, or real-data gates.
- Phase 13B completes Functional v1 with validated, business-scoped Activity filtering and stable bounded pagination plus a deterministic fictional pilot smoke verifier. Functional v1 is complete; deployment and real-data boundaries remain unchanged.
- Phase 13C, Pass 1 modernizes the visual foundation without changing product behavior: centralized calm-financial design tokens, compact icon-only application branding, a responsive application shell with desktop sidebar and accessible mobile drawer, and an executive Today dashboard. Today retains server-derived financial values, adds a proportional approved-cash/dedicated-reserve summary, prioritizes existing business-scoped attention counts, presents Weekly Review progress without hiding unresolved work, and uses a bounded ledger-backed activity feed. Screenshots and responsive checks are local-only artifacts; detailed visual treatment for remaining product pages is deferred to later reviewed passes.
- Phase 13C, Pass 2 corrects the shell breakpoint so the full sidebar starts at 1180px and no longer constrains the Today KPI layout at 1024px. It modernizes Money as a responsive transaction-review workspace with preserved filters, business-scoped table data, status/evidence context, and compatible Reconciliation/Journal navigation. It modernizes Documents as a secure lifecycle workspace with state-derived attention, validation/extraction/match/link indicators, protected-access-aware detail presentation, and no display of hashes, storage keys, grants, local paths, or internal identifiers. All financial calculations, document scanning, private access gates, human extraction/match review, business scope, and demo behavior remain unchanged.
- Phase 13C, Pass 1B refines shell branding and mobile navigation without changing routes or application data. Mobile has a safe-area-aware CaptureTracker header and persistent Today/Money/Review/Reports/More tab bar; More retains access to Taxes, Documents, Ask AI, Activity, and Settings through the shared destination list. The existing icon mark is proportioned directly on navy framing rather than inside an extra padded white badge. Semantic status colors, demo boundary, authentication, business scoping, and accounting behavior are unchanged.
- Phase 14A is a non-deploy production-readiness rehearsal. Its PostgreSQL 17.10 local fictional custom-archive destroy/restore proof passed with matching sanitized manifests; no provider, cloud resource, credential, deployment, or real data was used.
- Phase 14A.6 synchronized the Phase 14A documentation commits to `origin/main` and reviewed the authoritative Linux CI audit for `24b8994`. Its sanitized `runtime-audit-report.json` captured valid standard npm audit JSON with 0 critical, 6 high, and 1 moderate findings, all build-time, absent, outside the Worker request path, or not request-time reachable; runtime gate PASS (`clear-runtime`). Phase 14B remains NO-GO pending explicit human approvals and staging authorization.
- Gate 1B Linux Workerd review passed for `46ab914ae7d0c4769acc0df28852f4ca1b2ba88e` in successful workflow `30330025603`. The matching sanitized audit, inventory, direct-health-preflight, and full-proof artifacts reconcile to `clear-runtime`, the 2,278-byte / 734-byte-gzip Worker entry, 39 static assets, exact live `200` / fail-closed ready `503` health contracts, clean preview shutdown, and a successful non-interactive Wrangler dry run. Gate 1B is READY FOR GATE 2 only; provider access, resource creation, deployment, credentials, real data, and Phase 14B approval remain blocked. See `GATE_1B_LINUX_WORKERD_REVIEW.md`.
- Gate 2B created one Free fictional Neon staging project in Ohio and verified PostgreSQL 18.4. Twelve migrations, deterministic fictional seed, accounting balance (`9,263.00` debit/credit), business isolation, and the 15-relation catalog passed. A PostgreSQL 18 custom archive (`257,409` bytes) restored into the exact approved disposable local PostgreSQL 18 target with an exact sanitized manifest match; its archive was deleted. The post-proof Neon Billing console confirmed the Free plan, `$0/month` displayed cost, no paid plan/add-on, and no payment method. R2 remains blocked and unused.
- Gate 2C deployed only the existing fictional Worker `capture-tracker-staging` at `https://capture-tracker-staging.mjorider.workers.dev`. Commit `253c0aa` passed authoritative Linux workflow `30555414933`; deployed version `70a83efb-8024-4176-8098-8e1fb191e4ea` passed native/public liveness and readiness, 30 sequential plus 20 concurrent readiness requests, idle/post-smoke readiness, fictional smoke, accounting integrity, business isolation, unauthenticated boundaries, and local unavailable-database fail-closed proof. The package was 4,146.68 KiB gzip, below the Workers Paid 10 MiB limit. Exactly the expected three secret names were present without values. No R2, D1, KV, routes, domains, production, real data, or unrelated Worker changed; temporary bundles/workspaces were removed.
- Phase 13 staging UAT and production-readiness planning is documented in `PHASE_13_STAGING_UAT_AND_PRODUCTION_READINESS.md`. It requires fictional-only click-by-click UAT and separate written approvals for production infrastructure, real data, backup/restore, monitoring, document security, and the limited first-user pilot.

## Current boundary

Local/test data and staging are fictional-only. Production is undeployed, `CAPTURE_TRACKER_REAL_DATA_APPROVED=false`, and no real customer onboarding is allowed. One fictional Neon staging project and the separately deployed fictional `capture-tracker-staging` Worker exist. R2 remains deferred and is not declared for staging. The Gate 2B manual restore drill and post-proof `$0` cost confirmation passed; Gate 2C passed deployment and public fictional-staging proof.

## Recommended next work

**Phase 13 follow-up — execute and sign the fictional staging UAT package, resolve blocking findings, and obtain separate written production/real-data/pilot authorization only after the production-readiness checklist is complete.**

The existing fictional demo remains deterministic and has nine transactions, six original journal entries, a pending `$125.00` review, and no credentials until a later explicit fictional-staging bootstrap supplies a secure runtime-only fictional login. OneDrive Git cleanup stays disabled; the local integration suite remains intentionally isolated.

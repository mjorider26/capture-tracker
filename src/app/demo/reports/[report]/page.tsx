import { notFound } from "next/navigation"; import { AppShell } from "@/components/app-shell"; import { FinancialReports } from "@/components/financial-reports"; import { getFinancialReports } from "@/lib/data/reports"; import { resolveLocalDemoContext } from "@/lib/security/local-demo-context";
const valid = new Set(["profit-and-loss", "balance-sheet", "trial-balance", "cash-activity"]); export const dynamic = "force-dynamic"; export default async function DemoReportPage({ params, searchParams }: { params: Promise<{ report: string }>; searchParams: Promise<Record<string, string | string[] | undefined>> }) { const { report } = await params; const context = await resolveLocalDemoContext(); if (!context || !valid.has(report)) notFound(); return <AppShell mode="demo" destination="reports" businessName={context.businessName}><FinancialReports reports={await getFinancialReports(context.businessId, await searchParams)} basePath="/demo" focus={report as "profit-and-loss" | "balance-sheet" | "trial-balance" | "cash-activity"}/></AppShell>; }

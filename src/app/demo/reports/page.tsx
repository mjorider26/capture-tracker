import { notFound } from "next/navigation"; import { AppShell } from "@/components/app-shell"; import { FinancialReports } from "@/components/financial-reports"; import { getFinancialReports } from "@/lib/data/reports"; import { resolveLocalDemoContext } from "@/lib/security/local-demo-context";
export const dynamic = "force-dynamic"; export default async function DemoReportsPage({ searchParams }: { searchParams: Promise<Record<string, string | string[] | undefined>> }) { const context = await resolveLocalDemoContext(); if (!context) notFound(); return <AppShell mode="demo" destination="reports" businessName={context.businessName}><FinancialReports reports={await getFinancialReports(context.businessId, await searchParams)} basePath="/demo"/></AppShell>; }

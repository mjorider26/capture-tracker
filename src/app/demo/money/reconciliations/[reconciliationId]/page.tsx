import { notFound } from "next/navigation";
import { AppShell } from "@/components/app-shell"; import { AccountingNav } from "@/components/accounting-nav"; import { ReconciliationExperience } from "@/components/reconciliation-experience"; import { PageHeader } from "@/components/ui"; import { getReconciliationDetail } from "@/lib/data/reconciliations"; import { resolveLocalDemoContext } from "@/lib/security/local-demo-context"; import { finalizeDemoReconciliation, saveDemoReconciliation } from "./actions";
export const dynamic = "force-dynamic"; export const revalidate = 0; export const metadata = { robots: { index: false, follow: false } };
export default async function DemoReconciliationDetail({ params }: { params: Promise<{ reconciliationId: string }> }) { const context = await resolveLocalDemoContext(); if (!context) notFound(); const detail = await getReconciliationDetail(context.businessId, (await params).reconciliationId); if (!detail) notFound(); return <AppShell mode="demo" destination="money" businessName={context.businessName}><AccountingNav basePath="/demo" active="reconciliations"/><PageHeader eyebrow="Reconciliation evidence" title={detail.accountName} description={`Statement period ${new Date(detail.statementStartDate).toLocaleDateString()} to ${new Date(detail.statementEndDate).toLocaleDateString()}.`}/><ReconciliationExperience detail={detail} saveAction={saveDemoReconciliation} finalizeAction={finalizeDemoReconciliation}/></AppShell>; }

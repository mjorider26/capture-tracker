import { notFound } from "next/navigation"; import { AppShell } from "@/components/app-shell"; import { WeeklyReviewExperience } from "@/components/weekly-review-experience"; import { getWeeklyReview } from "@/lib/services/weekly-review"; import { resolveLocalDemoContext } from "@/lib/security/local-demo-context"; import { completeDemoWeeklyReview, reopenDemoWeeklyReview, startDemoWeeklyReview } from "./actions";
export const dynamic = "force-dynamic"; export default async function DemoReviewPage() { const context = await resolveLocalDemoContext(); if (!context) notFound(); const data = await getWeeklyReview(context.businessId); return <AppShell mode="demo" destination="review" businessName={context.businessName}><WeeklyReviewExperience review={data.review} attention={data.attention} startAction={startDemoWeeklyReview} completeAction={completeDemoWeeklyReview} reopenAction={reopenDemoWeeklyReview}/></AppShell>; }

import { notFound } from "next/navigation"; import { AppShell } from "@/components/app-shell"; import { FinancialReports } from "@/components/financial-reports"; import { getFinancialReports } from "@/lib/data/reports"; import { isAccessControlError, requireBusinessContext } from "@/lib/security/business-context";
export const dynamic = "force-dynamic"; export default async function ReportsPage({ searchParams }: { searchParams: Promise<Record<string, string | string[] | undefined>> }) { const context = await getContext(); return <AppShell mode="app" destination="reports" businessName={context.business.displayName}><FinancialReports reports={await getFinancialReports(context.business.id, await searchParams)} basePath="/app" canManageCpa={context.membership.role === "OWNER"}/></AppShell>; } async function getContext() { try { return await requireBusinessContext(); } catch (error) { if (isAccessControlError(error)) notFound(); throw error; } }

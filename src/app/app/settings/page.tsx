import { notFound } from "next/navigation"; import { AppShell } from "@/components/app-shell"; import { PilotSettings } from "@/components/pilot-experience"; import { isAccessControlError, requireBusinessContext } from "@/lib/security/business-context"; import { getPilotState } from "@/lib/services/pilot-readiness"; import { saveSettingsAction } from "./actions"; export default async function Settings(){const c=await context();return <AppShell mode="app" destination="settings" businessName={c.business.displayName}><PilotSettings state={await getPilotState(c.business.id)} action={saveSettingsAction}/></AppShell>}async function context(){try{return await requireBusinessContext()}catch(e){if(isAccessControlError(e))notFound();throw e}}

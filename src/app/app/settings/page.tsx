import { notFound } from "next/navigation"; import { AppShell } from "@/components/app-shell"; import { InstallCaptureTracker } from "@/components/install-capture-tracker"; import { PilotSettings } from "@/components/pilot-experience"; import { isAccessControlError, requireBusinessContext } from "@/lib/security/business-context"; import { getPilotState } from "@/lib/services/pilot-readiness"; import { saveSettingsAction } from "./actions"; export default async function Settings(){const c=await context();const state=await getPilotState(c.business.id);return <AppShell mode="app" destination="settings" businessName={c.business.displayName}><PilotSettings state={state} action={saveSettingsAction}/><section className="ui-card mt-5 max-w-2xl p-5"><h2 className="text-lg font-bold">Capture Tracker app</h2>{state.business?.customerExperience === "FOUNDING_CUSTOMER" && <p className="mt-2 text-sm font-bold text-brand-teal">Founding Customer · Customer #001</p>}<p className="mt-2 text-sm text-text-muted">Install Capture Tracker on your iPhone or iPad for a focused home-screen experience.</p><div className="mt-4"><InstallCaptureTracker /></div></section></AppShell>}async function context(){try{return await requireBusinessContext()}catch(e){if(isAccessControlError(e))notFound();throw e}}

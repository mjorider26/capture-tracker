import { notFound } from "next/navigation"; import { AppShell } from "@/components/app-shell"; import { isAccessControlError, requireBusinessContext } from "@/lib/security/business-context"; import { getPilotState } from "@/lib/services/pilot-readiness"; import { saveOnboardingAction } from "./actions"; import { SetupForm } from "@/components/pilot-experience"; export default async function Onboarding(){const c=await context(); const state=await getPilotState(c.business.id); return <AppShell mode="app" destination={null} businessName={c.business.displayName}><SetupForm state={state} action={saveOnboardingAction}/></AppShell>} async function context(){try{return await requireBusinessContext()}catch(e){if(isAccessControlError(e))notFound();throw e}}

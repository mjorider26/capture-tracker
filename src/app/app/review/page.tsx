import { notFound } from "next/navigation"; import { AppShell } from "@/components/app-shell"; import { WeeklyReviewExperience } from "@/components/weekly-review-experience"; import { getWeeklyReview } from "@/lib/services/weekly-review"; import { isAccessControlError, requireBusinessContext } from "@/lib/security/business-context"; import { completeAuthenticatedWeeklyReview, reopenAuthenticatedWeeklyReview, startAuthenticatedWeeklyReview } from "./actions";
export const dynamic = "force-dynamic";
export default async function ReviewPage() { const context = await getContext(); const data = await getWeeklyReview(context.business.id); return <AppShell mode="app" destination="review" businessName={context.business.displayName}><WeeklyReviewExperience review={data.review} attention={data.attention} startAction={startAuthenticatedWeeklyReview} completeAction={completeAuthenticatedWeeklyReview} reopenAction={reopenAuthenticatedWeeklyReview}/></AppShell>; }
async function getContext() { try { return await requireBusinessContext(); } catch (error) { if (isAccessControlError(error)) notFound(); throw error; } }

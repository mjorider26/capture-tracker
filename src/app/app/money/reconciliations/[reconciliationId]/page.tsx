import { notFound } from "next/navigation";
import { AppShell } from "@/components/app-shell"; import { AccountingNav } from "@/components/accounting-nav"; import { ReconciliationExperience } from "@/components/reconciliation-experience"; import { PageHeader } from "@/components/ui"; import { getReconciliationDetail } from "@/lib/data/reconciliations"; import { isAccessControlError, requireBusinessContext } from "@/lib/security/business-context"; import { finalizeAuthenticatedReconciliation, matchAuthenticatedStatementActivity, rejectAuthenticatedStatementCandidate, saveAuthenticatedReconciliation, unmatchAuthenticatedStatementActivity } from "./actions";
export const dynamic = "force-dynamic"; export const revalidate = 0; export const metadata = { robots: { index: false, follow: false } };
export default async function ReconciliationDetail({ params }: { params: Promise<{ reconciliationId: string }> }) { let context; try { context = await requireBusinessContext(); } catch (error) { if (isAccessControlError(error)) notFound(); throw error; } const detail = await getReconciliationDetail(context.business.id, (await params).reconciliationId); if (!detail) notFound(); return <AppShell mode="app" destination="money" businessName={context.business.displayName}><AccountingNav basePath="/app" active="reconciliations"/><PageHeader eyebrow="Reconciliation evidence" title={detail.accountName} description={`Statement period ${new Date(detail.statementStartDate).toLocaleDateString()} to ${new Date(detail.statementEndDate).toLocaleDateString()}.`}/><ReconciliationExperience detail={detail} saveAction={saveAuthenticatedReconciliation} finalizeAction={finalizeAuthenticatedReconciliation} matchAction={matchAuthenticatedStatementActivity} rejectAction={rejectAuthenticatedStatementCandidate} unmatchAction={unmatchAuthenticatedStatementActivity}/></AppShell>; }

"use client";
import { useActionState } from "react";
import type { ExtractionActionState } from "@/app/app/documents/actions";
const initial: ExtractionActionState = { ok: false };
type Action = (state: ExtractionActionState, data: FormData) => Promise<ExtractionActionState>;
type Attempt = { id: string; status: string; adapterId: string; adapterVersion: string; sourceSha256: string; completedAt: string | null; failureCode: string | null; candidates: Array<{ id: string; fieldType: string; originalValue: string; normalizedValue: string | null; confidence: string; reviewState: string; correctedValue: string | null }>; history: Array<{ id: string; action: string; createdAt: string }> };
export function DocumentExtractionPanel({ documentId, eligible, attempts, runAction, reviewAction }: { documentId: string; eligible: boolean; attempts: Attempt[]; runAction: Action; reviewAction: Action }) { const [runState, run] = useActionState(runAction, initial); const [reviewState, review] = useActionState(reviewAction, initial); const current = attempts[0]; return <section className="ui-card p-5"><h2 className="text-lg font-bold">Extraction review</h2><p className="mt-1 text-sm text-text-muted">Fictional development extraction — not production OCR. This evidence never updates a transaction or accounting record.</p>{eligible ? <form className="mt-4" action={run}><input type="hidden" name="documentId" value={documentId}/><button className="min-h-11 rounded bg-brand-navy px-4 text-sm font-bold text-white">Run fictional extraction</button></form> : <p className="mt-4 text-sm text-text-muted">Extraction is unavailable until this document is active, clean, privately stored, and readable.</p>}{runState.message && <p role={runState.ok ? "status" : "alert"} className="mt-3 text-sm">{runState.message}</p>}{current && <div className="mt-5 border-t border-border-subtle pt-5"><p className="text-sm font-semibold">{current.status.replaceAll("_", " ")} · {current.adapterId} v{current.adapterVersion}</p><p className="mt-1 break-all font-mono text-xs text-text-muted">Source {current.sourceSha256}</p>{current.failureCode && <p className="mt-2 text-sm text-red-800">Safe failure: {current.failureCode}</p>}{current.status === "STALE" && <p className="mt-2 text-sm text-[var(--warning)]">This extraction belongs to an older document version and cannot be reviewed.</p>}<ul className="mt-4 divide-y divide-border-subtle">{current.candidates.map((candidate) => <li key={candidate.id} className="py-3"><p className="text-sm font-bold">{candidate.fieldType.replaceAll("_", " ")} · {candidate.confidence}</p><p className="text-sm text-text-muted">{candidate.originalValue}{candidate.normalizedValue ? ` → ${candidate.normalizedValue}` : ""}</p><p className="mt-1 text-xs text-text-muted">{candidate.reviewState.replaceAll("_", " ")}{candidate.correctedValue ? ` · ${candidate.correctedValue}` : ""}</p>{candidate.reviewState === "UNREVIEWED" && current.status === "COMPLETED" && <div className="mt-2 flex flex-wrap gap-2"><form action={review}><input type="hidden" name="documentId" value={documentId}/><input type="hidden" name="candidateId" value={candidate.id}/><input type="hidden" name="review" value="ACCEPTED"/><button className="min-h-10 rounded border border-border-subtle px-3 text-sm font-bold">Accept</button></form><form action={review} className="flex gap-2"><input type="hidden" name="documentId" value={documentId}/><input type="hidden" name="candidateId" value={candidate.id}/><input type="hidden" name="review" value="CORRECTED"/><input required name="correctedValue" className="ui-input w-40" defaultValue={candidate.normalizedValue ?? candidate.originalValue}/><button className="min-h-10 rounded border border-border-subtle px-3 text-sm font-bold">Correct</button></form><form action={review}><input type="hidden" name="documentId" value={documentId}/><input type="hidden" name="candidateId" value={candidate.id}/><input type="hidden" name="review" value="REJECTED"/><button className="min-h-10 rounded border border-border-subtle px-3 text-sm font-bold">Reject</button></form></div>}</li>)}</ul><ol className="mt-4 space-y-1 text-xs text-text-muted">{current.history.map((event) => <li key={event.id}>{event.action.toLowerCase()} · {new Date(event.createdAt).toLocaleString()}</li>)}</ol></div>}{reviewState.message && <p role={reviewState.ok ? "status" : "alert"} className="mt-3 text-sm">{reviewState.message}</p>}</section>; }

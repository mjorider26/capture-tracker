const steps = ["Business", "Accounts", "Starting Books", "Owner Money", "Payroll", "Fixed Assets", "Reconcile", "Finish"] as const;
export function OnboardingProgress({ completed }: { completed: boolean }) { return <section aria-label="Setup progress" className="ui-card mb-6 p-5"><p className="text-xs font-bold uppercase tracking-[0.14em] text-brand-teal">Your setup path</p><ol className="mt-4 grid gap-2 sm:grid-cols-4">{steps.map((step, index) => <li key={step} className={`rounded border px-3 py-2 text-sm font-semibold ${completed || index === 0 ? "border-brand-teal/40 bg-brand-teal/10" : "border-border-subtle bg-surface-secondary"}`}><span className="mr-2 text-xs text-text-muted">{index + 1}</span>{step}</li>)}</ol></section>; }

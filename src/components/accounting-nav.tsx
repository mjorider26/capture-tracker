import Link from "next/link";

export function AccountingNav({ basePath, active }: { basePath: "/app" | "/demo"; active: "overview" | "import" | "bank" | "invoices" | "bills" | "reconciliations" | "journal" }) {
  const items = [{ key: "overview", label: "Overview", detail: "Financial operations", href: `${basePath}/money` }, { key: "import", label: "Import", detail: "CSV activity", href: `${basePath}/money/import` }, { key: "bank", label: "Bank", detail: "Connections", href: `${basePath}/money/bank` }, { key: "invoices", label: "Invoices", detail: "Get paid", href: `${basePath}/money/invoices` }, { key: "bills", label: "Bills", detail: "Payables", href: `${basePath}/money/bills` }, { key: "reconciliations", label: "Reconciliation", detail: "Statement evidence", href: `${basePath}/money/reconciliations` }, { key: "journal", label: "Journal activity", detail: "Immutable history", href: `${basePath}/money/journal` }] as const;
  return <nav aria-label="Money workspace sections" className="mb-7 flex gap-2 overflow-x-auto rounded-[var(--radius-md)] border border-border-subtle bg-surface p-1.5 shadow-[var(--shadow-subtle)]">{items.map((item) => <Link key={item.key} href={item.href} aria-current={active === item.key ? "page" : undefined} className={`min-h-12 shrink-0 rounded-[10px] px-3 py-2 text-left transition-colors sm:px-4 ${active === item.key ? "bg-brand-navy text-white shadow-sm" : "text-text-muted hover:bg-surface-secondary hover:text-text-primary"}`}><span className="block text-sm font-bold">{item.label}</span><span className={`mt-0.5 block text-[11px] ${active === item.key ? "text-slate-300" : "text-text-subtle"}`}>{item.detail}</span></Link>)}</nav>;
}

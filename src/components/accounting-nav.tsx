import Link from "next/link";
export function AccountingNav({ basePath, active }: { basePath: "/app" | "/demo"; active: "transactions" | "reconciliations" | "journal" }) { const items = [{ key: "transactions", label: "Transactions", href: `${basePath}/money` }, { key: "reconciliations", label: "Reconciliation", href: `${basePath}/money/reconciliations` }, { key: "journal", label: "Journal activity", href: `${basePath}/money/journal` }]; return <nav aria-label="Money sections" className="mb-6 flex gap-2 overflow-x-auto pb-1">{items.map((item) => <Link key={item.key} href={item.href} className={`min-h-11 shrink-0 rounded-[var(--radius-sm)] px-4 py-3 text-sm font-bold ${active === item.key ? "bg-brand-navy text-white" : "bg-surface-secondary text-text-muted"}`}>{item.label}</Link>)}</nav>; }

import Link from "next/link";
import { InstallCaptureTracker } from "./install-capture-tracker";

export function SetupCompleteCelebration({ ownerDisplayName, foundingCustomer, celebrate }: { ownerDisplayName: string; foundingCustomer: boolean; celebrate: boolean }) { return <section className="ui-card mx-auto max-w-2xl p-7 text-center"><p aria-hidden="true" className={celebrate ? "text-4xl motion-safe:animate-bounce" : "text-4xl"}>{foundingCustomer ? "🎉" : "✓"}</p><p className="mt-4 text-sm font-bold text-brand-teal">Setup complete</p><h1 className="mt-2 text-3xl font-bold">You’re live{ownerDisplayName ? `, ${ownerDisplayName.split(/\s+/)[0]}` : ""}.</h1><p className="mt-3 text-lg">Welcome to Capture Tracker{foundingCustomer ? ". Customer #001" : "."}</p><p className="mx-auto mt-3 max-w-lg text-sm leading-6 text-text-muted">Your S-Corp books are set up and ready to run in Capture Tracker.</p>{foundingCustomer && <p className="mt-4 text-sm font-bold text-brand-teal">Founding Customer · Customer #001</p>}<p className="mt-6 text-xs font-bold tracking-[0.12em] text-brand-teal">SPENDING TRACKED. BUSINESS GROWN.</p><div className="mt-7 flex flex-wrap justify-center gap-3"><Link href="/app/today" className="ui-button ui-button-primary min-h-11 px-4">Go to Today</Link><InstallCaptureTracker /></div></section>; }
